# Legacy Backup

This directory contains frozen reference material from the previous firmware.

Use it for:
- behavior discovery
- naming and coupling contrast
- legacy code evidence during refactors

Do not use it as:
- an active implementation target
- the place for normalized product specs
- a source of new architecture boundaries

Current normalized specs live under `docs/specs/`.
