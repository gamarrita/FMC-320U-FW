# FMC Derived Legacy Evidence

## Source And Artifact

The primary historical source is:
- `legacy/specs/fmc/use_cases.docx`.

The frozen derived artifact is:
- `legacy/derived/fmc/use_cases.extraction-v1.yaml`.

The YAML is an assisted structured extraction. It preserves the source's
conceptual mixture and may contain interpretations, omissions, or
normalizations introduced during extraction.

## Role And Preservation

The YAML is the preferred structured legacy reference. It is not a current
product specification and has no normative authority over the current product.
It remains frozen.

The Word source should be consulted only to audit a concrete discrepancy.
Discrepancies must be recorded. A material correction requires evaluating a new
extraction version rather than silently modifying `v1`.

Frozen YAML SHA-256:

```text
FBF4987193AC60777754136331B311BBDFF709FF818B6100B8FE246AF11662F2
```
